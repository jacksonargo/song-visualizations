name: deploy
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: checkout 📘
        uses: actions/checkout@v1
      - name: install deps 📦
        run: yarn install
      - name: build 🔧
        run: npm run build
      - name: deploy 🚀
        uses: JamesIves/github-pages-deploy-action@4.1.5
        with:
          branch: gh-pages
          folder: build
